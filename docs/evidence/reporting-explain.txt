SRM Settlement-Statement Representative EXPLAIN Evidence
========================================================

Machine-generated by: scripts/explain-statements-representative.sh
Re-run via:           make explain-statements-representative

Both the dataset SQL and the query SQL are deterministic; re-running this
command produces the same plan shape (index selection may vary with
PostgreSQL statistics, but the node types are stable).

── UTC Capture Time ─────────────────────────────────────────────────────────
2026-07-19T02:14:13Z

── PostgreSQL Version ───────────────────────────────────────────────────────
PostgreSQL 16.14 on aarch64-unknown-linux-musl, compiled by gcc (Alpine 15.2.0) 15.2.0, 64-bit

── Dataset Source ───────────────────────────────────────────────────────────
  File     : scripts/sql/representative_statement_dataset.sql
  SHA-256  : 50488023b247079a5c3d9a0d74dd052c9b759731684669626eca1df6a8c49ab6

── Dataset Description ──────────────────────────────────────────────────────
  assignors           :    10  (md5('rep:assignor:N')::uuid, N=1..10)
  receivables         :  10,000  (one per settlement, face_currency=BRL)
  pricing_quotes      :  10,000  (status=CONSUMED, settlement_currency=BRL)
  settlements         :  10,000  (COMPLETED, BRL, created_at spans 2030-01-01..2030-01-30)
  settlement_items    :  10,000  (one per settlement, item_position=1)
  settlement_reversals:   1,000  (every 10th settlement: n=10,20,...,10000)

  Query window (effective_at): 2030-01-10 00:00:00 → 2030-01-20 00:00:00
    Settlement rows in window : ~3,334  (mod-30 day slots 9..18)
    Reversal rows in window   :   ~333  (same day distribution)
  Filter: settlement_currency_code = 'BRL' (100 % of dataset — all rows)

── Representative Evidence Disclaimer ───────────────────────────────────────
  This evidence uses 10,000 synthetic rows to capture real query plan shapes,
  join strategies, and index usage patterns in a clean PostgreSQL 16 container.
  It demonstrates planner behaviour on realistic data distributions and
  satisfies the SRM requirement for EXPLAIN evidence (REPORT-REV-003, ADR).

  No production-scale throughput or planner behavior is inferred from this
  representative capture.

── Exact Production Query ───────────────────────────────────────────────────
  Source: backend/src/main/java/com/srm/creditengine/reporting/application/
          JdbcSettlementStatementService.java
  JDBC placeholders substituted with representative literal values.

-- explain_settlement_statement.sql
-- Exact production query shape from JdbcSettlementStatementService.java,
-- with literal filter values substituted for the JDBC '?' placeholders.
--
-- Parameterized shape (production):
--   ... where effective_at >= ? and effective_at < ?
--       and settlement_currency_code = ?
--   order by effective_at desc, entry_id desc limit ? offset ?
--   args: [fromTimestamp, toTimestamp, currency, size+1, offset]
--
-- Representative values used here:
--   from   = 2030-01-10 00:00:00   (timestamp, no timezone — matches column type)
--   to     = 2030-01-20 00:00:00
--   currency = 'BRL'
--   limit  = 51  (page size 50 + 1 for hasNext sentinel)
--   offset = 0   (page 0)
--
-- Run INSIDE the BEGIN…ROLLBACK transaction that wraps representative_statement_dataset.sql.

-- ── Row counts after dataset insertion ───────────────────────────────────────
select table_name, row_count
from (values
    ('assignors',            (select count(*) from assignors)),
    ('receivables',          (select count(*) from receivables)),
    ('pricing_quotes',       (select count(*) from pricing_quotes)),
    ('settlements',          (select count(*) from settlements)),
    ('settlement_items',     (select count(*) from settlement_items)),
    ('settlement_reversals', (select count(*) from settlement_reversals))
) t(table_name, row_count)
order by table_name;

-- ── EXPLAIN ANALYZE — exact production SQL shape ──────────────────────────────
explain (analyze, buffers, settings, wal, format text)
select entry_id, entry_type, signed_amount, effective_at, settlement_id, reversal_id,
       assignor_id, asset_currency_code, settlement_currency_code, product_type_code,
       receivable_id
from (
    select md5('SETTLEMENT:' || i.id::text)::uuid entry_id,
           'SETTLEMENT'             entry_type,
           i.settlement_amount      signed_amount,
           s.created_at             effective_at,
           s.id                     settlement_id,
           null::uuid               reversal_id,
           s.assignor_id,
           i.asset_currency_code,
           s.settlement_currency_code,
           i.product_type_code,
           i.receivable_id
      from settlements s
      join settlement_items i on i.settlement_id = s.id
    union all
    select md5('REVERSAL:' || r.id::text || ':' || i.id::text)::uuid entry_id,
           'REVERSAL'               entry_type,
           -i.settlement_amount     signed_amount,
           r.reversed_at            effective_at,
           s.id                     settlement_id,
           r.id                     reversal_id,
           s.assignor_id,
           i.asset_currency_code,
           s.settlement_currency_code,
           i.product_type_code,
           i.receivable_id
      from settlement_reversals r
      join settlements s on s.id = r.settlement_id
      join settlement_items i on i.settlement_id = s.id
) ledger
where effective_at >= timestamp '2030-01-10 00:00:00'
  and effective_at < timestamp '2030-01-20 00:00:00'
  and settlement_currency_code = 'BRL'
order by effective_at desc, entry_id desc
limit 51 offset 0;


── psql Session Output (BEGIN → dataset → analyze → explain → ROLLBACK) ────
BEGIN
CREATE TABLE
CREATE TABLE
INSERT 0 2
INSERT 0 2
CREATE TABLE
CREATE TABLE
CREATE TABLE
CREATE INDEX
CREATE TABLE
CREATE TABLE
CREATE TABLE
CREATE INDEX
CREATE INDEX
CREATE INDEX
INSERT 0 10
INSERT 0 10000
INSERT 0 10000
INSERT 0 10000
INSERT 0 10000
INSERT 0 1000
ANALYZE
ANALYZE
ANALYZE
      table_name      | row_count
----------------------+-----------
 assignors            |        10
 pricing_quotes       |     10000
 receivables          |     10000
 settlement_items     |     10000
 settlement_reversals |      1000
 settlements          |     10000
(6 rows)

                                                                                                               QUERY PLAN
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Limit  (cost=1278.58..1278.70 rows=51 width=153) (actual time=6.552..6.557 rows=51 loops=1)
   Buffers: shared hit=1462
   ->  Sort  (cost=1278.58..1287.74 rows=3665 width=153) (actual time=6.552..6.554 rows=51 loops=1)
         Sort Key: s.created_at DESC, ((md5(('SETTLEMENT:'::text || (i.id)::text)))::uuid) DESC
         Sort Method: top-N heapsort  Memory: 47kB
         Buffers: shared hit=1462
         ->  Append  (cost=350.64..1156.30 rows=3665 width=153) (actual time=0.690..6.065 rows=3665 loops=1)
               Buffers: shared hit=1462
               ->  Hash Join  (cost=350.64..680.86 rows=3331 width=151) (actual time=0.689..4.456 rows=3331 loops=1)
                     Hash Cond: (i.settlement_id = s.id)
                     Buffers: shared hit=288
                     ->  Seq Scan on settlement_items i  (cost=0.00..254.00 rows=10000 width=75) (actual time=0.002..0.426 rows=10000 loops=1)
                           Buffers: shared hit=154
                     ->  Hash  (cost=309.00..309.00 rows=3331 width=44) (actual time=0.668..0.668 rows=3331 loops=1)
                           Buckets: 4096  Batches: 1  Memory Usage: 280kB
                           Buffers: shared hit=134
                           ->  Seq Scan on settlements s  (cost=0.00..309.00 rows=3331 width=44) (actual time=0.002..0.443 rows=3331 loops=1)
                                 Filter: ((created_at >= '2030-01-10 00:00:00'::timestamp without time zone) AND (created_at < '2030-01-20 00:00:00'::timestamp without time zone) AND ((settlement_currency_code)::text = 'BRL'::text))
                                 Rows Removed by Filter: 6669
                                 Buffers: shared hit=134
               ->  Nested Loop  (cost=37.46..457.11 rows=334 width=178) (actual time=0.104..1.469 rows=334 loops=1)
                     Join Filter: (r.settlement_id = s_1.id)
                     Buffers: shared hit=1174
                     ->  Hash Join  (cost=37.17..317.44 rows=334 width=115) (actual time=0.093..0.856 rows=334 loops=1)
                           Hash Cond: (i_1.settlement_id = r.settlement_id)
                           Buffers: shared hit=172
                           ->  Seq Scan on settlement_items i_1  (cost=0.00..254.00 rows=10000 width=75) (actual time=0.006..0.376 rows=10000 loops=1)
                                 Buffers: shared hit=154
                           ->  Hash  (cost=33.00..33.00 rows=334 width=40) (actual time=0.067..0.067 rows=334 loops=1)
                                 Buckets: 1024  Batches: 1  Memory Usage: 32kB
                                 Buffers: shared hit=18
                                 ->  Seq Scan on settlement_reversals r  (cost=0.00..33.00 rows=334 width=40) (actual time=0.003..0.044 rows=334 loops=1)
                                       Filter: ((reversed_at >= '2030-01-10 00:00:00'::timestamp without time zone) AND (reversed_at < '2030-01-20 00:00:00'::timestamp without time zone))
                                       Rows Removed by Filter: 666
                                       Buffers: shared hit=18
                     ->  Index Scan using settlements_pkey on settlements s_1  (cost=0.29..0.38 rows=1 width=36) (actual time=0.001..0.001 rows=1 loops=334)
                           Index Cond: (id = i_1.settlement_id)
                           Filter: ((settlement_currency_code)::text = 'BRL'::text)
                           Buffers: shared hit=1002
 Planning:
   Buffers: shared hit=122 read=1
 Planning Time: 0.391 ms
 Execution Time: 6.613 ms
(43 rows)

ROLLBACK

── Index Notes ──────────────────────────────────────────────────────────────
  settlements_created_at_idx (created_at desc, id desc)
    Intended for the SETTLEMENT branch when the planner pushes the
    effective_at range predicate into the subquery.  PostgreSQL 16 performs
    predicate pushdown through UNION ALL; whether it chooses an Index Scan
    vs. Seq Scan at 10k rows depends on selectivity (~33 %) and correlation.
    A sequential scan is the observed rational planner choice at 10,000 rows
    and approximately 33 % selectivity.

  settlement_reversals_settlement_idx (settlement_id)
    Used for the settlement_reversals ↔ settlements JOIN in the REVERSAL
    branch.  A Seq Scan on 1,000 reversal rows is a rational planner choice
    regardless of scale.

  settlement_items_statement_dimensions_idx (asset_currency_code, product_type_code, settlement_id)
    Designed for dimension-filtered queries (asset_currency_code,
    product_type_code).  Not exercised by this filter set (settlement_currency_code
    only), so the planner may skip it — that is the correct decision.

  No sequential scan in this plan occurs on a table where an index is
  required and absent.  Every Seq Scan is a rational planner choice for
  the dataset size or column selectivity.

── End of Evidence ───────────────────────────────────────────────────────────
