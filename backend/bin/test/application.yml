spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:srm_credit_engine;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
  flyway:
    enabled: true
  sql:
    init:
      mode: never

management:
  endpoint:
    health:
      probes:
        enabled: true

srm:
  jwt-secret: srm-test-secret-do-not-use-32-bytes-minimum
